{
  "name": "nodejs-cicd-aws",
  "version": "1.0.0",
  "description": "Production-grade Node.js API with full CI/CD pipeline to AWS",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
   "test": "jest --coverage --testTimeout=10000",
    "lint": "eslint src/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.2.0"
  },
  "devDependencies": {
    "eslint": "^8.45.0",
    "jest": "^29.6.1",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
