{
  "name": "nodejs-cicd-aws",
  "version": "1.0.0",
  "description": "Production-grade Node.js API with full CI/CD pipeline to AWS",
  "main": "src/app.js",
 "scripts": {
  "test": "jest --coverage --testTimeout=10000 --forceExit",
  "lint": "eslint src/",
  "start": "node src/app.js"
},
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.45.0",
    "jest": "^29.6.1",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
